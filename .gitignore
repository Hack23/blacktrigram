# Dependencies
node_modules/

# Build output
/dist/
/lib/
/build/

# Test / coverage artifacts (regenerated by `npm run coverage` / `npm run test:e2e`)
/coverage/
/cypress/results/
/cypress/videos/
/cypress/screenshots/**/*

# TypeScript build info
*.tsbuildinfo

# Test results (root-level, not committed during PR work)
/test-results.xml

# Misc generated reports (not part of published docs/)
/mermaid-validation-report.json

# Playwright MCP
.playwright-mcp/
